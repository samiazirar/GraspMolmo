# ── Runtime libraries ───────────────────────────────────────────
numpy>=1.23
opencv-python>=4.10
open3d>=0.19
tqdm
requests            # handy if you swap in your own HTTP downloads

# ── GraspMolmo (with inference extras) ──────────────────────────
git+https://github.com/abhaybd/GraspMolmo.git